node_modules/
spike/dist/
spike/recall-calls.log
proxy/dist/
proxy/proxy.log.jsonl
.claude/settings.local.json
.env
eval/dist/
eval/smoke/dist/
# A run's own result documents. Ignored rather than excluded from the eval's dirty check, so that
# "uncommitted changes anywhere" stays literally true and a run's output cannot launder a dirty tree.
eval/results/
.claude/worktrees/
__pycache__/
*.pyc
eval/results/
